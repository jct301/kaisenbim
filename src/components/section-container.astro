---
interface Props {
  class?: string
  id?: string
}
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto container lg:max-w-4xl md:max-w-2xl px-6 md:px-0`}>
  <slot />
</section>
